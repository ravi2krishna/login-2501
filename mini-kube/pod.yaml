# create Nginx Pod
# kubectl run nginx-pod --image nginx --port=80
apiVersion: v1 # key: value
metadata: # dictionary
  name: nginx-pod
spec:
  container: # list
  - name: c1
  image: nginx
  ports:
  - containerPort: 80