# Create Nginx Pod
# kubectl run nginx-pod --image nginx --port=80
apiVersion: v1 # key: value 
kind: pod # key: value
metadata: # dictionary
  name: nginx-pod 
spec: # dictionary
  container: # list
  - name: c1
    image: nginx
  ports:
  - containerport: 80
