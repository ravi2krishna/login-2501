apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: RS-2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: c1
      image: nginx
      ports:
      - containerPort: 80